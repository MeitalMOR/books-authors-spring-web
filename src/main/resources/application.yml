spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/books_web
    username: root
    password: Meital123!
    driverClassName: com.mysql.cj.jdbc.Driver

  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
